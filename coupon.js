const coupon = {
    couponType: 'product || category  || total order',
    couponCode: 'string',
    couponProduct: 'string',
    date: 'date',
    discount: 'number',
    discountType: 'percent || amount || etc'
}

const couponCheck = {
    couponProductId: 'id',
    totalPrice: 'number',
}